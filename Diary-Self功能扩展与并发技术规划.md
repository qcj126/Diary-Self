# Diary-Self 功能扩展与并发技术规划

## 一、文档目标

本文档用于规划 Diary-Self 项目的后续功能扩展方向，以及多线程、异步、高并发相关技术在项目中的落地场景。

目标不是单纯堆功能，而是把项目升级成一个能体现工程能力的完整作品：

- 有清晰业务闭环：记录、目标、饮食、文件、AI、通知、社交互动。
- 有技术亮点：异步解耦、线程池、MQ、WebSocket、限流、缓存、定时任务。
- 有简历价值：每个扩展点都能对应一段可讲的项目经历。

## 二、项目定位升级方向

当前项目可以逐步从“个人记录工具”升级为：

> 一个融合日记记录、目标管理、饮食健康、AI 总结、实时通知和轻社交互动的个人成长平台。

推荐主线：

```text
用户上传饮食图片
  -> diary-file 异步上传 OSS
  -> RabbitMQ 通知 diary-AI 分析图片
  -> diary-AI 结构化识别营养数据
  -> diary-diet 写入饮食记录
  -> diary-notify 通过 WebSocket 推送处理完成
  -> diary-xxljob 每周生成饮食和目标成长报告
```

这条链路能串起文件服务、AI 服务、饮食服务、消息队列、实时通知和定时任务，是后续最推荐优先打造的“作品级链路”。

## 三、推荐新增模块

| 模块 | 作用 | 推荐优先级 |
|---|---|---|
| `diary-entry` | 日记正文、草稿、心情、隐私、日历视图 | 高 |
| `diary-social` | 点赞、评论、收藏、关注、动态广场 | 高 |
| `diary-message` | 站内信、消息中心、已读未读 | 高 |
| `diary-chat` | 单聊、群聊、聊天记录、离线消息 | 中 |
| `diary-habit` | 习惯、打卡、连续天数、补卡 | 中 |
| `diary-search` | 搜索日记、食谱、动态、目标 | 中 |
| `diary-statistics` | 数据统计、趋势分析、报表 | 中 |
| `diary-plan` | 饮食计划、目标计划、一周安排 | 中 |

## 四、功能扩展方向

### 4.1 日记模块 diary-entry

项目名叫 Diary-Self，建议补齐真正的日记模块，让项目主题更完整。

可扩展功能：

- 写日记、编辑日记、删除日记。
- 草稿箱。
- Markdown 编辑。
- 心情标签。
- 天气、地点、图片附件。
- 私密、公开、好友可见。
- 日历视图。
- 日记搜索。
- AI 情绪分析。
- AI 周/月总结。

可写入简历：

> 设计并实现日记核心模块，支持草稿、隐私权限、心情标签、图片附件和日历视图，并结合 AI 对用户日记进行情绪分析和周期总结。

### 4.2 社交互动模块 diary-social

适合加入点赞、评论、收藏、关注等互动能力。

可扩展功能：

- 动态广场：公开日记、食谱、目标成果、时间胶囊可发布到广场。
- 点赞/取消点赞。
- 评论和回复。
- 评论点赞。
- 收藏/取消收藏。
- 收藏夹分类。
- 关注/取消关注。
- 粉丝列表、关注列表。
- 好友申请。
- 拉黑用户。
- 热门动态排行。
- 点赞、评论、关注通知。

技术亮点：

- Redis Set 防止重复点赞。
- Redis 缓存点赞数、关注关系。
- RabbitMQ 异步更新计数和发送通知。
- Feed 流分页。
- 权限可见性控制。

可写入简历：

> 设计并实现轻社交互动模块，支持点赞、评论、收藏和关注关系，基于 Redis Set 实现点赞幂等校验，并通过 RabbitMQ 异步发送互动通知。

### 4.3 聊天模块 diary-chat

聊天功能可以复用 `diary-notify` 的 WebSocket/Netty 能力。

可扩展功能：

- 单聊。
- 群聊。
- 文本消息。
- 图片消息。
- 聊天记录分页查询。
- 已读/未读。
- 离线消息。
- 消息撤回。
- 消息 ACK 确认。
- 群公告、群成员管理、禁言。

技术亮点：

- Netty WebSocket 长连接。
- Channel 在线连接管理。
- 离线消息补偿。
- 消息幂等。
- ACK 确认机制。
- 消息顺序控制。

可写入简历：

> 基于 Netty WebSocket 实现实时聊天能力，支持单聊、离线消息、已读未读和 ACK 确认机制，提升系统实时交互能力。

### 4.4 AI 助手模块 diary-AI

AI 模块可以成为项目最有记忆点的部分。

可扩展功能：

- 饮食图片识别。
- 营养数据结构化解析。
- AI 日记总结。
- AI 目标复盘。
- AI 饮食建议。
- AI 周报/月报。
- 用户可向 AI 询问自己的历史记录。

示例问题：

- “我最近一周热量摄入怎么样？”
- “帮我总结这个月目标完成情况。”
- “根据我的饮食记录推荐一份晚餐。”
- “我最近目标拖延严重吗？”

技术亮点：

- 多模态 AI。
- Prompt 设计。
- 异步调用。
- 结果结构化解析。
- 失败重试。
- 多模块数据聚合。

可写入简历：

> 接入多模态 AI 能力，实现饮食图片自动识别与营养信息结构化解析，并通过 MQ 异步写入饮食记录，最终由 WebSocket 通知用户处理结果。

### 4.5 目标模块 diary-goal

可扩展功能：

- 目标进度自动计算。
- 子目标打卡。
- 学习时长统计。
- 目标到期提醒。
- 长时间未更新提醒。
- 目标模板广场。
- 复制目标模板。
- 目标完成报告导出。

技术亮点：

- 聚合计算。
- 事务控制。
- 乐观锁。
- XXL-JOB 定时扫描。
- RabbitMQ 通知解耦。
- 异步导出。

可写入简历：

> 基于 XXL-JOB 和 RabbitMQ 实现目标进度监控与到期提醒，支持目标完成率统计和异步导出报告。

### 4.6 饮食模块 diary-diet

可扩展功能：

- 饮食打卡。
- 早餐、午餐、晚餐、加餐分类。
- 营养趋势分析。
- 每日热量目标。
- 蛋白质、脂肪、碳水目标。
- AI 饮食建议。
- 食谱推荐。
- 一周饮食计划。
- 购物清单。

技术亮点：

- SQL 聚合统计。
- Redis 缓存热点统计。
- AI 个性化建议。
- 数据趋势图接口。

可写入简历：

> 实现饮食营养数据分析能力，支持多维度统计、目标预警和 AI 个性化建议生成。

### 4.7 文件模块 diary-file

该模块最适合沉淀高级工程能力。

可扩展功能：

- 大文件分片上传。
- 断点续传。
- 文件秒传。
- 图片压缩。
- 缩略图生成。
- 封面图生成。
- 水印图生成。
- 文件回收站。
- 延迟物理删除。
- 文件处理状态查询。

技术亮点：

- OSS 分片上传。
- 文件 Hash。
- Redis 维护上传进度。
- RabbitMQ 异步处理流水线。
- 死信队列。
- XXL-JOB 清理垃圾文件。

可写入简历：

> 设计并实现基于 OSS 的大文件分片上传与断点续传能力，通过文件 Hash 实现秒传，结合 Redis 维护上传进度，提升大文件上传稳定性。

### 4.8 通知模块 diary-notify

可扩展功能：

- WebSocket 在线用户管理。
- 多端登录连接管理。
- 心跳检测。
- 离线消息补偿。
- 通知 ACK 确认。
- 已读/未读。
- 统一消息中心。
- 点赞通知。
- 评论通知。
- 关注通知。
- 目标提醒。
- AI 处理完成通知。
- 文件处理完成通知。

技术亮点：

- Netty 高并发长连接。
- ConcurrentHashMap 管理 Channel。
- MQ 消息消费。
- 离线消息持久化。
- ACK 确认与重试。

可写入简历：

> 基于 Netty WebSocket 构建实时通知服务，支持在线连接管理、离线消息补偿和 ACK 确认机制。

### 4.9 用户模块 diary-user

可扩展功能：

- AccessToken + RefreshToken 双 Token。
- Token 黑名单。
- 强制下线。
- 多端登录管理。
- 查询当前登录设备。
- 登录失败次数限制。
- 验证码登录。
- IP 限流。
- 用户角色权限。

技术亮点：

- JWT。
- Redis Token 状态管理。
- Spring Security。
- 网关统一鉴权。
- 风控限流。

可写入简历：

> 设计基于 JWT + Redis 的用户认证体系，支持 Token 刷新、黑名单、强制下线和多端登录管理。

### 4.10 网关模块 diary-gateway

可扩展功能：

- 统一鉴权过滤器。
- 用户信息透传。
- 接口限流。
- 黑白名单。
- 跨域处理。
- TraceId 链路追踪。
- 统一请求日志。
- 灰度路由。

技术亮点：

- Spring Cloud Gateway。
- Redis 限流。
- JWT 校验。
- TraceId 透传。
- 全链路日志。

可写入简历：

> 基于 Spring Cloud Gateway 实现统一鉴权、接口限流和 TraceId 链路追踪，提升系统安全性与可观测性。

### 4.11 定时任务模块 diary-xxljob

可扩展功能：

- 图片垃圾清理。
- 上传失败文件补偿。
- 每日饮食提醒。
- 目标到期提醒。
- 时间胶囊开启提醒。
- 每周成长报告。
- 每月总结报告。

技术亮点：

- XXL-JOB。
- 分布式定时任务。
- 任务幂等。
- 失败重试。
- MQ 通知。

可写入简历：

> 基于 XXL-JOB 实现定时扫描、提醒推送和资源清理任务，通过幂等设计避免任务重复执行。

## 五、并发、异步、高并发技术落地

### 5.1 项目中适合使用并发技术的场景

| 场景 | 所属模块 | 推荐技术 |
|---|---|---|
| 图片批量上传 OSS | `diary-file` | `@Async`、线程池、`CompletableFuture`、`Semaphore` |
| 图片批量下载 | `diary-file` | `CompletableFuture.allOf`、线程池、`AtomicInteger` |
| 视频大文件上传 | `diary-file` | 分片上传、异步任务、限流 |
| AI 多图片识别 | `diary-AI` | `CompletableFuture`、`Semaphore`、MQ |
| WebSocket 长连接 | `diary-notify` | Netty EventLoop、并发容器 |
| 通知推送 | `diary-notify` | MQ、异步线程池、离线消息 |
| 图片清理任务 | `diary-xxljob` | `CompletableFuture`、`CountDownLatch`、`Semaphore` |
| 报告导出 | `diary-goal`、`diary-diet` | 异步导出、MQ、OSS |
| 网关限流 | `diary-gateway` | Redis、令牌桶、限流过滤器 |

普通新增、修改、删除、查询接口不建议为了使用高级技术而强行加线程或加锁。简单 CRUD 优先保证事务清晰、索引合理、代码可维护。

### 5.2 CompletableFuture

适合场景：

- 批量下载图片。
- 批量上传文件。
- 多张图片并发 AI 识别。
- 并发查询多个模块数据后生成报告。
- 多个导出分片完成后统一合并。

在本项目中的典型落点：

- `diary-file`：批量下载图片，等待所有下载任务完成后汇总成功/失败。
- `diary-AI`：对多张饮食图片并发调用 AI。
- `diary-goal`：导出报告时并发查询目标、子目标、统计数据。
- `diary-xxljob`：并发清理多个目录或多个 OSS 对象。

示例思路：

```java
List<CompletableFuture<Result>> futures = tasks.stream()
        .map(task -> CompletableFuture.supplyAsync(() -> handle(task), executor))
        .toList();

CompletableFuture.allOf(futures.toArray(new CompletableFuture[0])).join();

List<Result> results = futures.stream()
        .map(CompletableFuture::join)
        .toList();
```

注意事项：

- 一定要使用自定义线程池，不要随意使用公共 ForkJoinPool。
- 要处理异常，避免一个任务失败影响全部结果汇总。
- 不适合处理简单同步 CRUD。

### 5.3 CountDownLatch

适合场景：

- 一个大任务拆成多个子任务，主线程必须等待所有子任务完成后生成最终结果。
- 定时任务里并行扫描多个资源，最后统一输出任务报告。
- 批量清理任务中等待所有删除动作完成。

在本项目中的落点：

- `diary-xxljob` 图片清理任务。
- `diary-goal` 目标报告分片导出。
- `diary-diet` 多维度营养统计任务。

注意事项：

- Web 请求线程里不建议长时间 `await()`，否则会阻塞请求。
- 每个子任务必须在 `finally` 中调用 `countDown()`，否则主线程可能永远等待。

### 5.4 Semaphore

适合场景：

- 限制同时上传 OSS 的文件数量。
- 限制同时下载图片的数量。
- 限制同时调用 AI 接口的数量。
- 限制短信、邮件、外部 API 的并发请求数量。

在本项目中的落点：

- `diary-file`：最多同时上传 5 个文件到 OSS。
- `diary-file`：最多同时下载 10 张图片。
- `diary-AI`：最多同时调用 2 个 AI 请求。
- `diary-xxljob`：最多同时删除 10 个 OSS 对象。

示例思路：

```java
Semaphore semaphore = new Semaphore(5);

try {
    semaphore.acquire();
    uploadToOss(file);
} finally {
    semaphore.release();
}
```

注意事项：

- `release()` 必须写在 `finally` 中。
- `Semaphore` 适合做限流，不适合保护复杂业务一致性。
- 分布式系统中单机 `Semaphore` 只能限制单实例，跨实例限流应使用 Redis 或网关限流。

### 5.5 ReentrantLock

适合场景：

- 同一用户多端连接状态更新。
- 同一资源状态机流转。
- 同一文件上传状态更新。
- 同一导出任务防止重复启动。

在本项目中的落点：

- `diary-notify`：同一个用户连接替换、离线消息迁移时保护内存状态。
- `diary-file`：同一个文件上传任务状态流转。
- `diary-goal`：同一个目标导出任务避免重复执行。

注意事项：

- 能用数据库事务、唯一索引、乐观锁解决的，优先用数据库方案。
- 分布式多实例下，`ReentrantLock` 只保护当前 JVM，不保护整个系统。
- 跨服务锁应考虑 Redis 分布式锁或数据库锁。

示例思路：

```java
private final ReentrantLock lock = new ReentrantLock();

public void updateState() {
    lock.lock();
    try {
        doUpdate();
    } finally {
        lock.unlock();
    }
}
```

### 5.6 @Async 和线程池

项目中已经有线程池配置：

- `ossUploadExecutor`
- `ossDownloadExecutor`
- `nettyTaskExecutor`

适合继续扩展：

- `aiInvokeExecutor`：AI 调用专用线程池。
- `reportExportExecutor`：报告导出专用线程池。
- `notifyPushExecutor`：通知推送专用线程池。

建议原则：

- 不同类型任务使用不同线程池隔离。
- IO 密集任务线程数可以稍大。
- CPU 密集任务线程数接近 CPU 核心数。
- 设置队列容量和拒绝策略。
- 打好线程名前缀，方便排查问题。

### 5.7 RabbitMQ

适合做模块解耦：

- 文件上传完成通知 AI。
- AI 分析完成通知饮食模块。
- 饮食记录生成后通知消息中心。
- 目标到期提醒通知用户。
- 点赞、评论、关注后异步发送通知。

推荐消息类型：

| 消息 | 说明 |
|---|---|
| `FILE_UPLOAD_SUCCESS` | 文件上传完成 |
| `AI_ANALYSIS_COMPLETE` | AI 分析完成 |
| `GOAL_DUE` | 目标到期 |
| `DIET_REMIND` | 饮食提醒 |
| `SOCIAL_LIKE` | 点赞通知 |
| `SOCIAL_COMMENT` | 评论通知 |
| `TIME_CAPSULE_OPEN` | 时间胶囊开启 |

注意事项：

- 消费端要做幂等。
- 失败消息进入死信队列。
- 关键消息需要手动 ACK。
- 消息体建议带业务唯一 ID。

### 5.8 Redis

适合场景：

- 登录 Token 黑名单。
- 验证码。
- 点赞去重。
- 关注关系缓存。
- 热门动态排行。
- 接口限流。
- 文件上传进度。
- 用户在线状态。

推荐结构：

| 场景 | Redis 结构 |
|---|---|
| 点赞去重 | Set |
| 热门排行 | ZSet |
| 验证码 | String + TTL |
| 上传进度 | Hash |
| Token 黑名单 | String + TTL |
| 用户在线状态 | Hash |

## 六、推荐优先实现路线

### 第一阶段：补齐产品核心

目标：让项目主题更完整。

推荐功能：

1. `diary-entry` 日记模块。
2. 日记图片附件。
3. 日记隐私权限。
4. 心情标签。
5. 日历视图。

### 第二阶段：做出互动感

目标：让项目从个人工具升级为轻社区。

推荐功能：

1. `diary-social` 点赞。
2. 评论和回复。
3. 收藏。
4. 关注关系。
5. 动态广场。

### 第三阶段：接入实时通知

目标：把 `diary-notify` 真正用起来。

推荐功能：

1. 统一消息中心。
2. 点赞通知。
3. 评论通知。
4. 关注通知。
5. WebSocket 实时推送。
6. 离线消息补偿。

### 第四阶段：打造 AI 链路

目标：形成最有简历价值的业务闭环。

推荐功能：

1. 饮食图片上传。
2. OSS 异步上传。
3. MQ 通知 AI 分析。
4. AI 识别食物和营养数据。
5. 自动写入饮食记录。
6. WebSocket 通知用户处理完成。

### 第五阶段：工程能力增强

目标：把项目从“能用”提升到“工程化”。

推荐功能：

1. 网关统一鉴权。
2. Redis 接口限流。
3. TraceId 链路追踪。
4. XXL-JOB 定时报告。
5. OSS 垃圾文件清理。
6. 大文件分片上传和秒传。

## 七、最适合写进简历的项目亮点

可以按下面方式组织简历描述：

1. **微服务架构**

   > 项目采用多模块微服务架构，拆分用户、文件、饮食、目标、AI、通知、网关和定时任务模块，提升系统边界清晰度与扩展能力。

2. **异步文件处理**

   > 基于自定义线程池和 RabbitMQ 实现文件上传异步处理，接口快速返回文件 ID，后台完成 OSS 上传、状态回写和消息通知。

3. **AI 饮食识别**

   > 接入多模态 AI 实现饮食图片识别，将图片中的食物和营养信息结构化入库，并通过 WebSocket 推送处理结果。

4. **实时通知**

   > 基于 Netty WebSocket 构建实时通知服务，支持在线连接管理、离线消息补偿和 ACK 确认机制。

5. **高并发控制**

   > 使用 CompletableFuture 并发处理批量文件任务，并结合 Semaphore 控制 OSS 和 AI 外部接口调用并发，避免资源耗尽。

6. **网关与安全**

   > 基于 Spring Cloud Gateway 实现统一鉴权、Redis 限流和 TraceId 链路追踪，提升系统安全性和可观测性。

7. **定时任务**

   > 基于 XXL-JOB 实现目标提醒、饮食报告、文件清理等定时任务，并通过幂等设计保证任务重复执行安全。

## 八、推荐优先做的三个功能

如果时间有限，最推荐优先做这三个：

### 1. diary-entry 日记模块

原因：

- 项目主题最需要它。
- 功能完整度提升明显。
- 后续社交、AI、搜索都能基于日记展开。

### 2. diary-social 点赞、评论、收藏、关注

原因：

- 能快速让项目有互动属性。
- 可以练 Redis、MQ、消息通知。
- 简历上比普通 CRUD 更好讲。

### 3. AI 饮食识别链路

原因：

- 技术含金量最高。
- 能串起多个模块。
- 面试时非常容易展开。

## 九、注意事项

- 不要为了使用并发工具而强行引入复杂度。
- 简单 CRUD 优先保证事务、索引和代码结构。
- 外部接口调用、文件处理、批量任务、通知推送才是异步和并发的主要场景。
- 单机锁只能保护当前服务实例，微服务场景要考虑分布式锁或数据库约束。
- MQ 消费必须考虑幂等、重试和死信队列。
- WebSocket 推送不要阻塞 Netty IO 线程。
- 线程池必须设置名称、核心线程数、最大线程数、队列容量和拒绝策略。

## 十、建议的项目最终形态

最终可以把 Diary-Self 打造成：

> 一个面向个人成长的智能生活记录平台，支持日记记录、目标管理、饮食健康、文件存储、AI 分析、实时通知、轻社交互动和周期成长报告。

这样的项目既有业务完整性，也有后端技术深度，适合在简历和面试中重点展示。
