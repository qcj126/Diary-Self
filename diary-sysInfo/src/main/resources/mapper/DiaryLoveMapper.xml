<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="diary.diaryinfo.mapper.DiaryLoveMapper">
    <resultMap id="loveMoodResultMap" type="diary.common.entity.love.po.LoveMoodPO">
        <id property="id" column="id"/>
        <result property="moodCode" column="mood_code"/>
        <result property="moodName" column="mood_name"/>
        <result property="emoji" column="emoji"/>
        <result property="sort" column="sort"/>
        <result property="enabled" column="enabled"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <resultMap id="loveRecordMoodResultMap" type="diary.common.entity.love.po.LoveRecordMoodPO">
        <id property="id" column="id"/>
        <result property="recordId" column="record_id"/>
        <result property="moodId" column="mood_id"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="loveCoupleColumns">
        id, owner_user_id, partner_user_id, partner_name, start_date, status, deleted, create_time, update_time
    </sql>
    <sql id="loveAnniversaryColumns">
        id, couple_id, creator_user_id, name, event_date, repeat_type, remind_days, pinned, sort, deleted, create_time, update_time
    </sql>
    <sql id="loveLocationColumns">
        id, couple_id, name, address, longitude, latitude, city_code, city_name, deleted, create_time, update_time
    </sql>
    <sql id="loveRecordColumns">
        id, couple_id, creator_user_id, location_id, title, content, record_date, category_code, important, sort, deleted, create_time, update_time
    </sql>
    <sql id="loveRecordImageColumns">
        id, record_id, image_id, is_cover, sort, create_time
    </sql>
    <sql id="loveMoodColumns">
        id, mood_code, mood_name, emoji, sort, enabled, create_time, update_time
    </sql>
    <sql id="loveRecordMoodColumns">
        id, record_id, mood_id, sort, create_time
    </sql>
    <insert id="insertLoveMood">
        INSERT INTO love_mood (id, mood_code, mood_name, emoji, sort, enabled)
        VALUES (#{id}, #{moodCode}, #{moodName}, #{emoji}, #{sort}, #{enabled})
    </insert>

    <select id="selectLoveMoods" resultMap="loveMoodResultMap">
        SELECT <include refid="loveMoodColumns"/> FROM love_mood
        <if test="enabled != null">WHERE enabled = #{enabled}</if>
        ORDER BY sort ASC, id ASC
    </select>

    <update id="updateLoveMood">
        UPDATE love_mood SET
            mood_code = #{moodCode}, mood_name = #{moodName}, emoji = #{emoji}, sort = #{sort},
            enabled = #{enabled}, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteLoveMood">
        DELETE FROM love_mood WHERE id = #{id}
    </delete>

</mapper>
