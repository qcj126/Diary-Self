<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="diary.recipe.mapper.RecipeMapper">

    <resultMap id="BaseResultMap" type="diary.common.entity.recipe.po.RecipePO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="image_id" property="imageId"/>
        <result column="description" property="description"/>
        <result column="category_num" property="categoryNum"/>
        <result column="meal_type" property="mealType"/>
        <result column="difficulty" property="difficulty"/>
        <result column="cook_way" property="cookWay"/>
        <result column="cooking_time" property="cookingTime"/>
        <result column="story" property="story"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, user_id, title, image_id, description, category_num, meal_type,
        difficulty, cook_way, cooking_time, story, sort, red_heart, family_member, create_time, update_time
    </sql>

    <sql id="PageWhereCondition">
        <where>
            <if test="req.categoryNum != null">
                AND category_num = #{req.categoryNum}
            </if>
            <if test="req.mealType != null">
                AND meal_type = #{req.mealType}
            </if>
            <if test="req.difficulty != null">
                AND difficulty = #{req.difficulty}
            </if>
            <if test="req.redHeart != null">
                AND red_heart = #{req.redHeart}
            </if>
            <if test="req.keyword != null and req.keyword != ''">
                AND (title LIKE CONCAT('%', #{req.keyword}, '%')
                     OR description LIKE CONCAT('%', #{req.keyword}, '%'))
            </if>
        </where>
    </sql>

    <insert id="insert" parameterType="diary.common.entity.recipe.po.RecipePO">
        INSERT INTO recipe (
            id, user_id, title, image_id, description, category_num, meal_type,
            difficulty, cook_way, cooking_time, story, sort, family_member, create_time, update_time
        ) VALUES (
            #{id}, #{userId}, #{title}, #{imageId}, #{description}, #{categoryNum}, #{mealType},
            #{difficulty}, #{cookWay}, #{cookingTime}, #{story}, #{sort}, #{familyMember}, #{createTime}, #{updateTime}
        )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM recipe
        WHERE id = #{recipeId}
    </select>

    <update id="updateById">
        UPDATE recipe
        <set>
            <if test="req.title != null and req.title != ''">title = #{req.title},</if>
            <if test="req.imageId != null">image_id = #{req.imageId},</if>
            <if test="req.description != null and req.description != ''">description = #{req.description},</if>
            <if test="req.categoryNum != null">category_num = #{req.categoryNum},</if>
            <if test="req.mealType != null">meal_type = #{req.mealType},</if>
            <if test="req.difficulty != null">difficulty = #{req.difficulty},</if>
            <if test="req.cookWay != null">cook_way = #{req.cookWay},</if>
            <if test="req.cookingTime != null">cooking_time = #{req.cookingTime},</if>
            <if test="req.story != null">story = #{req.story},</if>
            <if test="req.familyMember != null">family_member = #{req.familyMember},</if>
            <if test="req.redHeart != null">red_heart = #{req.redHeart},</if>
            update_time = NOW()
        </set>
        WHERE id = #{recipeId}
    </update>

    <select id="selectByAuthor" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM recipe
        WHERE user_id = #{authorId}
        ORDER BY sort ASC, create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="search" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM recipe
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%')
                 OR description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="categoryNum != null">AND category_num = #{categoryNum}</if>
        <if test="mealType != null">AND meal_type = #{mealType}</if>
        <if test="difficulty != null">AND difficulty = #{difficulty}</if>
        ORDER BY sort ASC, create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countSearch" resultType="int">
        SELECT COUNT(*)
        FROM recipe
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%')
                 OR description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="categoryNum != null">AND category_num = #{categoryNum}</if>
        <if test="mealType != null">AND meal_type = #{mealType}</if>
        <if test="difficulty != null">AND difficulty = #{difficulty}</if>
    </select>

    <update id="batchUpdate">
        <foreach collection="list" item="item" separator=";">
            UPDATE recipe
            <set>
                <if test="item.title != null and item.title != ''">title = #{item.title},</if>
                <if test="item.description != null and item.description != ''">description = #{item.description},</if>
                <if test="item.categoryNum != null">category_num = #{item.categoryNum},</if>
                <if test="item.mealType != null">meal_type = #{item.mealType},</if>
                <if test="item.difficulty != null">difficulty = #{item.difficulty},</if>
                <if test="item.cookingTime != null">cooking_time = #{item.cookingTime},</if>
                <if test="item.story != null">story = #{item.story},</if>
                <if test="item.sort != null">sort = #{item.sort},</if>
                update_time = NOW()
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM recipe
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectByAuthorTitle" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM recipe
        WHERE title = #{title}
          AND meal_type = #{mealType}
        LIMIT 1
    </select>

    <select id="qryPage" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM recipe
        <include refid="PageWhereCondition" />
        ORDER BY sort ASC, create_time DESC, id DESC
    </select>

    <delete id="deleteRecipeById">
        DELETE FROM recipe WHERE id = #{recipeId}
    </delete>
</mapper>
