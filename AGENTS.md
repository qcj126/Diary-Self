# 项目规则
- 修改代码前先了解现有结构。
- 保持改动范围最小，不修改无关文件。
- 新增功能必须补充测试。
- 项目使用 Java 21 和 Maven 多模块结构，以根 pom.xml 为版本和模块信息的唯一依据。
- 单模块改动优先运行：mvn -pl <模块名> -am test。
- 跨模块依赖、公共模块或根 pom.xml 发生变化时，运行：mvn test。
- 新增或修改业务逻辑时补充对应测试；修复缺陷时优先添加能够复现缺陷的回归测试。
- 无法运行测试时，必须说明未运行的原因和已完成的替代验证；不得声称未实际执行的检查已经通过。
- 未经确认，不添加新的生产依赖。
- 每个功能开发时，增删改查必须放在专用的service中，如LoveAddService，LoveQueryService等。

# 后端代码风格
- 使用清晰、具有语义的命名。
- 根据职责、可读性、复用性和可测试性决定是否抽取方法，不单纯以代码行数判断。
- 新增、修改或删除前端可调用接口时，同步更新根目录的《前端调用后端参数文档.md》。
- 文档至少包含请求方式、请求地址、入参名称、类型、是否必填、含义以及返回结果。
- 不涉及接口行为变化的内部重构，无须修改接口文档。
- 严格遵守阿里巴巴java开发规范。
- 创建实体类时，必须添加字段注释。
- 公共实体统一放入 diary-common/src/main/java/diary/common/entity/。
- 公共枚举、常量、异常、转换器分别放入 diary-common 对应包。
- 优先复用现有工具方法。只有无业务语义、无状态且具有跨模块复用价值的方法才能加入公共工具diary-utils中；业务专用逻辑应留在所属模块。
- 配置类默认放在使用该配置的业务模块下，只有确实被多个模块共享时才放公共模块。
- 校验入参参数时，除开异步任务，统一在impl层使用MyUtils.check()校验。
- 业务异常统一抛出项目自定义异常，由 GlobalExceptionHandler 转换为前端响应。
- 只有在能够恢复、降级、补充上下文或转换异常时才使用 try-catch，禁止捕获后静默忽略。
- @Transactional 方法捕获异常后，若事务必须回滚，应重新抛出运行时异常，或使用 rollbackFor = Exception.class。
- 需要在同一事务中操作的多张表，必须新建专用Service层，并使用@Transactional注解；若已有此Service，那么请将此方法放入此Service中。
- 请使用MyUtils工具类中的方法，不要自己随意写工具类，若无此方法，请自行添加。
- MyBatis XML 中的自定义查询必须使用显式 resultMap，namespace 必须与 Mapper 全限定类名一致，statement id 必须与 Mapper 方法名一致。

# 聊天规则
- 所有回复使用中文。
- 使用一问一答式交流，当你觉得有90%把握能一次性开发完功能时，即可动手开发。
- 在使用较新技术时，必须联网查询资料。

# 兼容性与安全
- 未经确认，不得执行破坏性数据库操作，不得删除或覆盖用户数据。
- 数据库表结构、公共接口、RocketMQ topic/tag/消息结构、Redis key 格式发生变化前，必须先说明兼容性和迁移方案并等待确认。
- 不得在代码、配置、日志或文档中写入真实密码、Token、AccessKey 或其他凭据。
- 不修改无关文件，不进行全仓库格式化，不提交构建产物。