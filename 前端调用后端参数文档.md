# 前端调用后端参数文档

更新时间：2026-08-06

说明：本文档按“厨房创食记、阶段目标、饮食记录、时光机”四个界面整理当前前端调用后端接口时传递的参数，同时列出前端界面自身需要维护的参数。参数示例中的 `null` 表示当前无值或不限制。

## 厨房创食记

### 1. 查询食谱

接口：`POST /recipe/query`

当前传给后端的参数：

```json
{
  "pageIndex": 1,
  "pageSize": 20,
  "category": null,
  "mealType": null,
  "difficulty": null,
  "redHeart": null,
  "keyword": ""
}
```

参数说明：

| 参数 | 类型 | 来源 | 简要说明 |
| --- | --- | --- | --- |
| pageIndex | number | 固定值/分页 | 当前页码，当前页面默认传 `1`。 |
| pageSize | number | 固定值/分页 | 每页数量，当前页面默认传 `20`。 |
| category | number/null | 左侧分类按钮 `activeCategory` | 食谱分类；为 `null` 时不按分类筛选。 |
| mealType | number/null | 接口封装预留 | 餐次类型，当前页面没有筛选控件，默认传 `null`。 |
| difficulty | number/null | 接口封装预留 | 难度筛选，当前页面没有筛选控件，默认传 `null`。 |
| redHeart | number/null | “红心食谱”按钮 `anniversaryOnly` | 是否只查纪念/红心食谱；按钮开启时传 `1`，关闭时传 `null`。 |
| keyword | string | 搜索框 `keyword` | 标题关键字搜索；为空字符串时不按关键字筛选。 |

前端需要的参数：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| todayData | object | 当前列表展示数据，包含日期和食谱数组。 |
| categories | array | 左侧食谱分类按钮数据。 |
| selectedRecipe | object/null | 当前打开详情页的食谱。 |
| currentView | string | 当前视图，`timeline` 或 `detail`。 |
| recipeLoading | boolean | 查询加载状态。 |
| recipeSaving | boolean | 新增/修改/删除保存状态。 |
| recipeError | string | 接口失败时展示的错误信息。 |
| recipeNotice | string | 保存、删除成功后的提示信息。 |
| keyword | string | 搜索框内容，同时作为 `keyword` 传给后端。 |
| activeCategory | number/null | 当前选中的食谱分类，同时作为 `category` 传给后端。 |
| anniversaryOnly | number/null | 红心食谱筛选状态，同时作为 `isAnniversary` 传给后端。 |
| categoryDialogOpen | boolean | 新增分类弹窗开关，当前只影响前端本地分类。 |
| categoryDraft.label | string | 新增分类名称，当前未调用后端。 |
| categoryDraft.icon | string | 新增分类图标，当前未调用后端。 |

### 2. 新增/修改食谱

接口：`POST /recipe/add`、`POST /recipe/update`

当前传给后端的参数：

```json
{
  "authorId": null,
  "recipeId": null,
  "title": "",
  "imageId": "",
  "description": "",
  "category": null,
  "mealType": null,
  "difficulty": null,
  "cookingTime": null,
  "servings": null,
  "story": "",
  "ingredients": [
    {
      "name": "",
      "quantity": "",
      "isMain": 0,
      "sort": 1
    }
  ],
  "steps": [
    {
      "stepNumber": 1,
      "description": "",
      "timerMin": 0,
      "sort": 1
    }
  ]
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| authorId | number/null | 作者 ID，当前由食谱对象传入，默认 `null`。 |
| recipeId | number/string/null | 食谱 ID；新增为 `null`，修改时传已有 ID。 |
| title | string | 食谱标题。 |
| imageId | string | 封面图片 ID，来自图片上传返回值。 |
| description | string | 食谱描述。 |
| category | number/null | 食谱分类。 |
| mealType | number/null | 餐次类型。 |
| difficulty | number/null | 难度。 |
| cookingTime | number/null | 烹饪时间，单位分钟。 |
| servings | number/null | 份量。 |
| story | string | 食谱故事/备注。 |
| ingredients | array | 食材明细。 |
| ingredients[].name | string | 食材名称。 |
| ingredients[].quantity | string | 食材数量/用量。 |
| ingredients[].isMain | number | 是否主食材，`1` 是，`0` 否。 |
| ingredients[].sort | number | 食材排序。 |
| steps | array | 做法步骤。 |
| steps[].stepNumber | number | 步骤序号。 |
| steps[].description | string | 步骤描述。 |
| steps[].timerMin | number | 步骤计时分钟数。 |
| steps[].sort | number | 步骤排序。 |

### 3. 删除食谱

接口：`POST /recipe/delete`

当前传给后端的参数：

```json
{
  "recipeId": null
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| recipeId | number/string | 要删除的食谱 ID。 |

### 4. 上传/查询食谱图片

接口：`POST /file/upload/images`

当前传给后端的参数：

```json
{
  "files": ["File"],
  "code": "2000"
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| files | File[] | 上传的图片文件列表，使用 `multipart/form-data`。 |
| code | string | 图片业务类型，食谱图片固定传 `2000`。 |

接口：`POST /file/query/images/urls`

当前传给后端的参数：

```json
["imageId"]
```

参数说明：传图片 ID 数组，用于换取图片 URL。

## 阶段目标

### 1. 查询阶段目标

接口：`POST /goal/query`

当前传给后端的参数：

```json
{
  "userId": null,
  "creator": "",
  "category": 1,
  "title": "",
  "recentDays": null,
  "exactDate": "",
  "ddl": 1
}
```

参数说明：

| 参数 | 类型 | 来源 | 简要说明 |
| --- | --- | --- | --- |
| userId | number/null | 接口封装/session 预留 | 用户 ID，当前查询默认传 `null`。 |
| creator | string | 创建人输入框 | 按创建人筛选。 |
| category | number | 分类下拉框 | `1` 表示全部，`2` 起对应前端分类：技术、学习、健康、生活。 |
| title | string | 标题输入框 | 按标题关键字筛选。 |
| recentDays | number/null | 最近天数输入框 | 筛选最近 N 天更新的数据。 |
| exactDate | string | 日期选择器 | 精确日期，格式 `YYYY-MM-DD`。 |
| ddl | number | DDL 状态下拉框 | `1` 全部，`2` 正常，`3` 即将到期，`4` 已到期。 |

前端需要的参数：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| filters.creator | string | 查询创建人，同时传给后端。 |
| filters.category | number | 查询分类，同时传给后端。 |
| filters.title | string | 查询标题，同时传给后端。 |
| filters.recentDays | number/null | 最近更新时间天数，同时传给后端。 |
| filters.exactDate | string | 精确日期，同时传给后端。 |
| filters.ddl | number | DDL 状态，同时传给后端。 |
| queryPanelOpen | boolean | 查询面板开关。 |
| exactDateInput | ref | 日期选择器 DOM 引用。 |
| goals | array | 阶段目标列表。 |
| selectedIds | array | 勾选的阶段目标 ID，用于修改/删除。 |
| expandedIds | array | 展开的阶段目标 ID。 |
| selectedSubGoalKeys | array | 勾选的小目标 Key。 |
| loading | boolean | 查询加载状态。 |
| saving | boolean | 新增/修改保存状态。 |
| deleting | boolean | 删除状态。 |
| editorOpen | boolean | 新增/修改弹窗开关。 |
| editorMode | string | `create` 或 `edit`。 |
| editingId | number/string/null | 当前修改的阶段目标 ID。 |
| draft.title | string | 阶段目标标题。 |
| draft.category | string | 阶段目标分类。 |
| draft.content | string | 阶段目标内容。 |
| draft.subcategories | array | 新增/修改阶段目标时携带的小目标草稿。 |
| subGoalDrafts | array | 批量新增小目标弹窗草稿。 |
| exportForm.exportType | number | 导出类型，同时传给后端。 |
| exportForm.lastDays | number | 导出过去天数，同时传给后端。 |
| exportForm.exportSize | number | 导出条数，同时传给后端。 |

### 2. 新增/修改阶段目标

接口：`POST /goal/add`、`POST /goal/update`

当前传给后端的参数：

```json
{
  "id": null,
  "userId": null,
  "creator": "",
  "category": "技术",
  "title": "",
  "description": "",
  "subGoals": [
    {
      "id": null,
      "stageId": null,
      "title": "",
      "content": "",
      "learnedHours": 0,
      "estimatedHours": 0
    }
  ]
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| id | number/string/null | 阶段目标 ID；新增为 `null`，修改为已有 ID。 |
| userId | number/null | 当前登录用户 ID，来自 session。 |
| creator | string | 创建人，来自 session 用户名。 |
| category | string | 阶段目标分类。 |
| title | string | 阶段目标标题。 |
| description | string | 阶段目标内容。 |
| subGoals | array | 小目标列表。 |
| subGoals[].id | number/string/null | 小目标 ID。 |
| subGoals[].stageId | number/string/null | 所属阶段目标 ID。 |
| subGoals[].title | string | 小目标标题。 |
| subGoals[].content | string | 小目标学习内容。 |
| subGoals[].learnedHours | number | 已学习时长。 |
| subGoals[].estimatedHours | number | 预计用时。 |

### 3. 批量新增小目标

接口：`POST /goal/batch/addSubGoal`

当前传给后端的参数：

```json
[
  {
    "stageId": null,
    "userId": null,
    "title": "",
    "content": "",
    "learnedHours": 0,
    "estimatedHours": 0
  }
]
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| stageId | number/string | 所属阶段目标 ID。 |
| userId | number/null | 当前登录用户 ID。 |
| title | string | 小目标标题。 |
| content | string | 小目标学习内容。 |
| learnedHours | number | 已学习时长，新增时默认 `0`。 |
| estimatedHours | number | 预计用时。 |

### 4. 删除阶段目标

接口：`POST /goal/delete/{goalId}`

当前传给后端的参数：

```json
{}
```

路径参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| goalId | number/string | 要删除的阶段目标 ID，拼接在 URL 路径中。 |

### 5. 导出阶段目标

接口：`POST /goal/export?exportType=1&lastDays=7&exportSize=10`

当前传给后端的参数：

```json
{
  "exportType": "1",
  "lastDays": "7",
  "exportSize": "10"
}
```

参数说明：这些参数以 URL Query 形式传递，不放在 JSON body 中。

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| exportType | string/number | 导出类型，`1` PDF，`2` 图片，`3` Excel。 |
| lastDays | string/number | 导出过去 N 天，`0` 表示全部。 |
| exportSize | string/number | 导出目标条数。 |

## 饮食记录

当前状态：`DietPage.vue` 使用前端静态数组 `meals`，没有调用后端接口。因此当前传给后端的参数为空。

当前传给后端的参数：

```json
{}
```

前端需要的参数：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| activeSegment | string | 当前时间分段，可能值：`today`、`week`、`month`、`habit`。当前只影响前端显示状态，未传给后端。 |
| search | string | 搜索关键字，用于前端过滤餐次、菜名、描述、备注、心情。 |
| meals | array | 饮食记录静态数据列表。 |
| filteredMeals | array | 根据 `search` 过滤后的饮食记录。 |
| meal.id | number/string | 饮食记录 ID。 |
| meal.type | string | 餐次类型，如 `breakfast`、`lunch`、`snack`、`dinner`。 |
| meal.period | string | 餐次中文名称。 |
| meal.time | string | 就餐时间。 |
| meal.name | string | 菜品名称。 |
| meal.desc | string | 菜品描述。 |
| meal.comment | string | 备注。 |
| meal.kcal | number | 热量，单位 kcal。 |
| meal.protein | number | 蛋白质。 |
| meal.carbs | number | 碳水。 |
| meal.fat | number | 脂肪。 |
| meal.mood | string | 饮食状态/心情。 |
| meal.img | string | 图片 URL。 |

若后续接入后端，建议查询接口至少传递以下参数：

```json
{
  "period": "today",
  "keyword": "",
  "pageIndex": 1,
  "pageSize": 20
}
```

说明：`period` 对应 `activeSegment`，`keyword` 对应 `search`，分页参数用于后端列表分页。

## 时光机

### 本次代码调整记录

本次发现时光机查询面板新增/已有条件只在前端过滤，没有传给后端。已修改代码，将查询按钮和重置按钮接入 `/time-machine/card/query`，并将以下参数一并传给后端：`daysAgo`、`exactDate`、`categoryId`、`pageIndex`、`pageSize`。其中 `daysAgo` 和 `exactDate` 是本次新增到后端请求 DTO 的参数。

### 1. 查询分类

接口：`POST /time-machine/category/query`

当前传给后端的参数：

```json
{
  "id": null,
  "userId": 10000,
  "categoryName": "",
  "deleted": 0,
  "sort": 0
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| id | number/string/null | 分类 ID，查询时默认 `null`。 |
| userId | number/string | 用户 ID，当前使用 `GLOBAL_USER_ID` 或事件/分类对象上的用户 ID。 |
| categoryName | string | 分类名称，查询时默认空字符串。 |
| deleted | number | 删除状态，`0` 未删除，`1` 已删除。 |
| sort | number | 排序值，查询时默认 `0`。 |

### 2. 新增/修改/删除分类

接口：`POST /time-machine/category/add`、`POST /time-machine/category/update`、`POST /time-machine/category/delete`

当前传给后端的参数：

```json
{
  "id": null,
  "userId": 10000,
  "categoryName": "",
  "deleted": 0,
  "sort": 0
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| id | number/string/null | 分类 ID；新增为 `null`，修改/删除为已有 ID。 |
| userId | number/string | 用户 ID。 |
| categoryName | string | 分类名称。 |
| deleted | number | 删除状态；删除接口默认传 `1`。 |
| sort | number | 分类排序。 |

### 3. 查询卡片

接口：`POST /time-machine/card/query`

当前传给后端的参数：

```json
{
  "id": null,
  "userId": 10000,
  "imageId": null,
  "categoryId": null,
  "cardTitle": "",
  "cardContent": "",
  "recordTime": "2026-08-06T00:00:00.000Z",
  "deleted": 0,
  "pageIndex": 1,
  "pageSize": 30,
  "daysAgo": null,
  "exactDate": ""
}
```

参数说明：

| 参数 | 类型 | 来源 | 简要说明 |
| --- | --- | --- | --- |
| id | number/string/null | DTO 默认值 | 查询时默认 `null`。 |
| userId | number/string | `readUserId()` | 用户 ID。 |
| imageId | number/string/null | DTO 默认值 | 查询时默认 `null`。 |
| categoryId | number/string/null | 分类下拉 `queryFilters.categoryKey` 转换 | 分类 ID；全部分类传 `null`。 |
| cardTitle | string | DTO 默认值 | 查询时默认空字符串。 |
| cardContent | string | DTO 默认值 | 查询时默认空字符串。 |
| recordTime | string | DTO 默认值 | 未指定时由 DTO 默认填当前时间。 |
| deleted | number | 固定值 | 删除状态，查询未删除数据传 `0`。 |
| pageIndex | number | 页码输入框 `queryFilters.page` | 当前页码。 |
| pageSize | number | 每页数量下拉 `queryFilters.pageSize` | 每页数量，可选 30、60、90、120、150、180、210、240、270、300。 |
| daysAgo | number/null | 距今天数输入框 `queryFilters.daysAgo` | 距今天数筛选；为空时传 `null`。本次新增传给后端。 |
| exactDate | string | 日期选择器 `queryFilters.exactDate` | 精确日期，格式 `YYYY-MM-DD`；为空字符串时不限制。本次新增传给后端。 |

### 4. 新增/修改/删除卡片

接口：`POST /time-machine/card/add`、`POST /time-machine/card/update`、`POST /time-machine/card/delete`

当前传给后端的参数：

```json
{
  "id": null,
  "userId": 10000,
  "imageId": null,
  "categoryId": null,
  "cardTitle": "",
  "cardContent": "",
  "recordTime": "2026-08-06T00:00:00.000Z",
  "deleted": 0,
  "pageIndex": 1,
  "pageSize": 25,
  "daysAgo": null,
  "exactDate": ""
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| id | number/string/null | 卡片 ID；新增为 `null`，修改/删除为已有 ID。 |
| userId | number/string | 用户 ID。 |
| imageId | number/string/null | 图片 ID。 |
| categoryId | number/string/null | 所属分类 ID。 |
| cardTitle | string | 卡片标题。 |
| cardContent | string | 卡片内容。 |
| recordTime | string | 记录时间，保存时由事件日期转换而来。 |
| deleted | number | 删除状态；删除接口默认传 `1`。 |
| pageIndex | number | DTO 默认分页字段，新增/修改/删除时默认 `1`。 |
| pageSize | number | DTO 默认分页字段，新增/修改/删除时默认 `25`。 |
| daysAgo | number/null | DTO 查询筛选字段，新增/修改/删除时默认 `null`。 |
| exactDate | string | DTO 查询筛选字段，新增/修改/删除时默认空字符串。 |

前端需要的参数：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| categories | array | 分类列表。 |
| allEvents | array | 全部卡片事件。 |
| selectedCategory | string | 左侧当前选中的分类 Key。 |
| selectedSubcategory | string/null | 当前选中的子分类 Key。 |
| selectedEventId | string/null | 当前详情弹窗打开的卡片 ID。 |
| selectedEventIds | array | 勾选的卡片 ID，用于批量删除/修改入口。 |
| isAddingEvent | boolean | 新增卡片弹窗状态。 |
| isEditingEvent | boolean | 编辑卡片弹窗状态。 |
| addingCategory | object/null | 新增卡片时默认所属分类。 |
| categoryDraft.label | string | 新增分类名称。 |
| categoryDraft.icon | string | 新增分类图标，目前用于前端显示，分类 DTO 暂未传 `icon`。 |
| queryPanelOpen | boolean | 查询面板开关。 |
| queryFilters.daysAgo | number/null | 距今天数，本次已传给后端。 |
| queryFilters.exactDate | string | 精确日期，本次已传给后端。 |
| queryFilters.categoryKey | string | 查询分类 Key，会转换成 `categoryId` 传给后端。 |
| queryFilters.pageSize | number | 每页数量，本次已传给后端。 |
| queryFilters.page | number | 页码，本次已传给后端。 |
| pageSizeOptions | number[] | 每页数量下拉选项。 |
| currentPageEvents | array | 当前页面展示的卡片列表。 |
| filteredEventsCount | number | 当前筛选后数量。 |
| hasMore | boolean | 是否还有更多后端数据。 |
| toast | object | 页面提示状态。 |

### 5. 上传/查询时光机图片

接口：`POST /file/upload/images`

当前传给后端的参数：

```json
{
  "files": ["File"],
  "imageDTO": {
    "code": 9000,
    "userId": 10000
  },
  "code": "9000",
  "userId": "10000"
}
```

参数说明：

| 参数 | 类型 | 简要说明 |
| --- | --- | --- |
| files | File[] | 上传的图片文件列表，使用 `multipart/form-data`。 |
| imageDTO.code | number | 图片业务类型，由分类映射得到，如日常为 `9000`。 |
| imageDTO.userId | number/string | 用户 ID。 |
| code | string | 同步追加的业务类型字段。 |
| userId | string | 同步追加的用户 ID 字段。 |

接口：`POST /file/query/images/urls`

当前传给后端的参数：

```json
["imageId"]
```

参数说明：传图片 ID 数组，用于换取图片 URL。

接口：`POST /file/query/images/carousel`

当前传给后端的参数：

```json
{}
```

参数说明：当前轮播图查询不传 body。
