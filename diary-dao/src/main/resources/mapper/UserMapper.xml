<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="diary.dao.mapper.user.UserMapper">

    <resultMap id="UserResultMap" type="diary.dao.entity.user.po.User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id, username, email, phone, password, status, create_time, update_time
    </sql>

    <select id="selectByEmail" resultMap="UserResultMap">
        select <include refid="Base_Column_List"/> from user where email = #{identify} and status = '1000'
    </select>

    <select id="selectByPhone" resultMap="UserResultMap">
        select <include refid="Base_Column_List"/> from user where phone = #{identify} and status = '1000'
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        select <include refid="Base_Column_List"/> from user where username = #{identify} and status = '1000'
    </select>

    <insert id="userRegister" parameterType="diary.dao.entity.user.po.User">
        insert into user (user_id, username, email, phone, password)
        values (#{userId}, #{username}, #{email}, #{phone}, #{password})
    </insert>

    <update id="updatePassword">
        update user set password = #{password} where username = #{username}
    </update>
</mapper>
